// Stub for non-JS targets — fetch_request only exists on the js backend.
fn main {
  println("This example requires the js target. Build with:")
  println("  moon run --target js examples/rpc-fetch")
}
